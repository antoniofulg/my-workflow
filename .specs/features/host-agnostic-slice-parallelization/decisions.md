# Host-Agnostic Slice Parallelization Decisions

## Human decisions

| Decision | Why | Alternatives rejected | Change cost now | User cost today |
| --- | --- | --- | --- | --- |
| Use coordinator-assisted Orca workers until automatic orchestration is fixed. | Direct worktree creation, prompt delivery, and same-terminal follow-up provide useful inter-slice overlap now. | Wait for the upstream fix; remain fully serial. Both discard available wall-time savings. | Remove the assisted section after automatic execution proves equivalent recovery and cleanup. | Main agent must supervise checkpoints and cleanup. |
| Keep TLC and readiness stages unchanged. | Parallelism must reduce wall time without reducing confidence. | Parallel tasks inside a slice; skipped or combined Verifier, review, gate, or QA stages. | None; existing contracts already enforce this. | Some dependencies still serialize and sync can repeat affected gates. |

## Autonomous run decisions

| Decision | Why | Alternatives rejected | Change cost now | User cost today |
| --- | --- | --- | --- | --- |
| Extend existing Slice D before its first PR. | Assisted execution is an adoption/runtime contract extension and the feature is not merged. Frozen slice count and role route remain valid. | Open a stacked feature before merging this branch; silently refresh the frozen workflow. | Split T5 into a follow-up feature and PR. | One additional Slice D verification and incremental grouped review. |
| Use Orca worktree comments as restart handoffs, not as machine receipts. | Native state survives agent turns and can be reconciled against Git and `tasks.md` without new code. | Add a second scheduler or parse free-form comments as authoritative state. | Add a structured ledger only if assisted recovery proves insufficient. | Coordinator performs reconciliation after restart. |
| Keep automatic Orca `1.4.188` preflight unsupported. | Assisted supervision cannot prove transactional worker lifecycle semantics. | Write a compatibility PASS or route the automatic adapter through direct terminal polling. | Remove known-bad version only after upstream release plus canary PASS. | Automatic `safe/full` remains serial until Orca updates. |

AD-017 records the cross-feature ownership boundary.

## QA Execute Retest 10 — human decisions

| Decision | Why | Alternatives rejected | Change cost now | User cost today |
| --- | --- | --- | --- | --- |
| Move every frozen role off Codex onto `claude`, keeping the implementer at `sonnet` / `low`. | The Codex account exhausted its weekly plan limit during Retest 9 and does not reset until 2026-09-01; the run cannot wait. | Wait for the Codex quota; move only the implementer (Retest 9's plan) — the second still halts at Technical Verification or Deep Review on the same exhausted account. | Re-freeze `workflow.json` and re-walk; the snapshot is one file and the walk is the expensive part. | The `low` implementer effort is the same setting that failed task integrity in Retest 7, so the route's task contract is unproven and each retest costs a full live pilot run. |
| Fail closed rather than weaken a proof to get past a provider limitation. | A proof relaxed to fit a provider stops being evidence for every future provider. | Accept one screen frame; drop the effort assertion; shrink the packet until it happens to arrive. | None — nothing was relaxed. | Two consecutive cycles have ended without reaching the open newline Major. |

## QA Execute Retest 10 — run decisions

| Decision | Why | Alternatives rejected | Change cost now | User cost today |
| --- | --- | --- | --- | --- |
| Derive the Claude route matcher empirically from the ground worktree's promoted startup shell before creating any slice worktree. | The retest-8/9 matcher was Codex TUI text and would have failed closed on a correct route; guessing the Claude banner would have been an unverified matcher. | Port the Codex matcher by analogy; assert only provider and model and drop effort. | Trivial — the matcher is six string checks in `retest-10/orca_probe.py`. | One extra worktree-scoped discovery pass (42 samples, ~75 s) before the walk. |
| Require `Claude Code` + `Sonnet 5` + `with low effort` on two consecutive `source=screen` frames, and require the other two efforts absent. | Claude Code renders all three frozen facts on one banner line, so the contract's full tuple proof is directly satisfiable on this provider. | Weaken to provider+model because "effort may not render" — untrue here and it would have hidden a real mismatch. | None. | None; both slices accepted at sample 4. |
| Send every packet as one physical line instead of the wrapped multi-line shape Retests 6-9 used on Codex. | A newline typed into the Claude Code TUI input can submit the message early, splitting one logical packet into several sends and breaking the one-send rule. | Send the wrapped shape and hope for bracketed paste; calibrate multi-line behaviour on a live slice worker and burn the lane. | Trivial — packet files are authored per retest. | None observed; the failure that did occur is length-related, not newline-related. |
| Characterize the truncation on the ground worktree's non-slice shell rather than on a slice worker. | "Every counted claim carries the command that produced the number", and the ground shell carries no slice contract, so no lane's one-send rule was spent on diagnosis. | Report the root cause from the two burned lanes alone (unmeasured); re-send to a slice worker to test (forbidden). | None. | One extra prompt on a worktree that was about to be destroyed. |
| Treat the truncation as a Critical product/contract defect rather than a QA harness limitation. | `orca terminal send --help` exposes no chunking, paste or stdin mode, so `--text` is the only expressible transport; the receipt reports a complete `bytesWritten` the agent never receives; and the contract forbids the retry that would recover. Retests 2-4 were harness bugs because a *different* correct invocation existed — here none does. | File it as "invalid / not exercised" like Retests 2-4, which would leave a Critical contract hole unrecorded for an eleventh cycle. | Reversible by one bug-status edit if the contract owner shows a supported transport QA missed. | The assisted path is blocked on the Claude route until delivery is provable. |
| Halt after the two rejected packets instead of re-walking with shorter packets in the same session. | The contract forbids a replacement worker or second terminal for a slice, and both slices were burned; loss is timing-dependent, so a shorter packet is only *less likely* to truncate, never proven intact. | Create fresh A/B worktrees and retry smaller (forbidden replacement workers, and it would manufacture a pass that the next real packet would not reproduce). | A fresh Verifier session can re-walk once delivery is provable. | Grouped Deep Review, the newline fix loop, final persona QA, and the fixture full gate are unreached for the second consecutive cycle. |
| Record `retest_status: fail` and keep `qa_status: fail`, adding the new bug id rather than replacing the newline bug. | Both defects are live and distinct; the newline Major is unreached, not resolved, and the transport Critical is what makes it unreachable. | `blocked-verify` — wrong, since the gap is a product defect a fix can close, not a leg only a human can walk. | None. | Two open bugs against one scenario, in dependency order. |

## QA Execute Retest 11 — human decisions

| Decision | Why | Alternatives rejected | Change cost now | User cost today |
| --- | --- | --- | --- | --- |
| Keep the implementer on `claude` / `sonnet` / `low` for this walk. | Retest 7's task-integrity failure on a low-effort worker was never reproduced or cleared, and only a live walk could answer it. | Raise the effort before walking, retiring the question instead of answering it. | Superseded mid-run by the raise below. | One walk spent on a route that was then superseded — though it did produce the clean-integrity observation that informs the raise. |
| Solve the transport defect on our side; do not patch or fork Orca. | The host defect belongs upstream, and a fork would make this workflow depend on a private build. | Fork Orca; wait for an upstream `terminal send` acknowledgement mode; keep shrinking inline packets until they happen to fit. | Revert `AD-018` if Orca ships an acknowledging transport. | The coordinator owns packet-file lifecycle and the worker must read a file before acting. |
| Move every frozen role off Codex (carried forward from Retest 10). | The Codex account's weekly plan limit does not reset until 2026-09-01. | Wait for the quota; keep verifier and deep_reviewer on Codex and halt mid-run. | Re-freeze `workflow.json`. | None observed this run. |
| **Abort Retest 11 and raise the implementer effort from `low` to `medium`.** | The frozen implementer route is being superseded, so this walk's evidence cannot close the scenario; continuing would spend tokens producing evidence against a route that no longer exists. | Let Retest 11 finish and record its result anyway, which would put a verdict against a dead route into the tracker. | `workflow.json` is re-frozen and Retest 12 walks the new route. | A complete ~59-minute walk recorded as `invalid / not exercised`; the scenario stays `untested` for an eleventh cycle. |

## QA Execute Retest 11 — run decisions

| Decision | Why | Alternatives rejected | Change cost now | User cost today |
| --- | --- | --- | --- | --- |
| Prove pointer delivery on the ground worktree's non-slice shell with a 2418-character packet before creating any slice worktree. | The whole retest depended on delivery working; discovering otherwise on a slice would burn a lane under the one-send rule. The size was chosen to exceed both packets Retest 10 lost and to sit near its 2081-character payload that lost 98.3 %. | Start the walk and find out on `A_T1`; probe with a small packet that proves nothing about the sizes that failed. | None — one file and one prompt. | ~30 s on a worktree that was going to be destroyed. This is the one finding that survives the abort. |
| Require the probe packet to echo an embedded random token plus the first and last positional filler markers. | A worker can plausibly respond to a truncated tail; only content from the middle and both ends proves the whole body arrived. | Ask the worker "did you get it?" — an answer, not evidence. | None. | None. |
| Send the pointer through a fixed-argv, `shell=False` wrapper without applying `shq`. | The contract names fixed-argv/no-shell as the preferred form; no shell parses the payload, and quoting would inject literal quote characters into the TUI input, which the contract forbids. `packet_file` is interpolated raw exactly once and never quoted twice. | Apply `shlex.quote` to the pointer literally, wrapping a space-containing string in single quotes the TUI would receive verbatim. | None. | None. |
| Treat the two `-a` worktrees as a coordinator error, record it in full, clean both exactly, and restart slice A under a fresh unique logical name. | The second create was mine — I re-ran the command to read its stderr, which is exactly the blind retry the contract forbids. Hiding it would corrupt the record the run exists to produce. | Adopt the first `-a` and carry on quietly; declare the retest invalid and halt immediately. | None; both were removed with 10/10 reconstructed ownership proofs. | Two disposable worktrees created and destroyed, and one honest blemish in the record. |
| Classify the 15 s create timeout and the 10 s concurrent-read timeouts as harness limits, not product defects, and raise them. | Both are retest-2 constants, not contract values; the contract's own bound is the 60 s settle window, and Orca's effects were correct and late, not wrong. | File a product bug against `orca worktree create`; keep the tight timeouts and let the run keep crashing. | Two constants in `retest-11/orca_probe.py`. | None; `create`, `send`, `rm`, `set` and `stop` are still never retried. |
| Give fresh Technical Verifiers their own terminals via `orca terminal create` rather than re-`exec`ing on the slice's startup handle. | `exec` replaced the shell, so exiting the worker agent would kill the handle the cleanup contract must still revalidate. Orca's own help names `terminal create` as the way to get a fresh agent in an existing checkout. | Re-`exec` on the startup handle and lose it; reuse the worker session as its own verifier, which is not a fresh actor. | None. | The sole-handle cleanup proof became "startup handle plus exactly one recorded auxiliary handle". |
| Read the Deep Review verdict and counts back from the generated artifacts rather than from the reviewer agent's final line. | A reviewer reporting its own verdict is a claim standing in for a command. | Trust the `DEEP_REVIEW_DONE` marker the packet asks the agent to emit. | None. | One extra read of `state.json`, `review-stats.json` and `findings.json`. |
| On abort, run the full cleanup lifecycle with no shortcut and re-verify the baseline live afterwards. | An abort is exactly when residue leaks, and the direction said so. | Trust the cleanup that had already run and skip re-verification. | None. | One extra inventory pass. |
| On abort, revert the two bug closures and the `pass` verdict this run had drafted, and keep only the `retest_status` correction. | A verdict against a superseded route must not enter the tracker, and the direction said not to touch either bug record. `retest_status: fail` alongside `fix_status: pending` is a recording error under `QA-SCENARIOS.md`, independent of any run's result. | Keep the closures because the evidence was real; revert everything including the field correction, leaving a documented inconsistency in place. | One frontmatter edit when the bugs genuinely close. | The truncation Critical and the newline Major both stay open despite this run's contrary evidence, which Retest 12 will have to re-establish. |

## QA Execute Retest 12 — human decisions

| Decision | Why | Alternatives rejected | Change cost now | User cost today |
| --- | --- | --- | --- | --- |
| **Raise the frozen implementer effort from `sonnet`/`low` to `sonnet`/`medium`.** | Handed down mid-Retest-11. Retest 7's task-integrity failure happened on a low-effort worker, and Retest 11's single clean low observation came from a run recorded `invalid / not exercised`, so the low route's task contract was never durably proven. | Keep `low` and rely on Retest 11's observation — a verdict resting on a run that explicitly closes nothing. | `workflow.json` is one file; the walk is the expensive part. | One superseded ~59-minute walk (Retest 11), and this scenario stayed `untested` for an eleventh cycle before Retest 12 closed it. |
| **Every frozen role stays off Codex** (carried forward from Retests 10 and 11). | The Codex account's weekly plan limit does not reset until 2026-09-01. | Wait for the quota; split roles across providers and halt mid-run at Technical Verification or Deep Review. | Re-freeze `workflow.json`. | None observed — all six terminals routed on Claude without incident. |
| **Solve the transport defect on our side; do not patch or fork Orca** (carried forward from Retest 11). | The host defect belongs upstream, and a fork would make this workflow depend on a private build. | Fork Orca; wait for an upstream `terminal send` acknowledgement mode; keep shrinking inline packets until they happen to fit. | Revert `AD-018` if Orca ships an acknowledging transport. | The coordinator owns packet-file lifecycle and the worker must read a file before acting. |
| **Resolve everything and run to the end; a halt report naming what stopped the run is a legitimate result, an invented verdict is not.** | An unattended run that stops at a phase boundary costs exactly what the human was trying to avoid. | Report after Deep Review for acknowledgement; declare PASS on partial evidence. | None. | None — the run reached a closed scenario. |

## QA Execute Retest 12 — run decisions

| Decision | Why | Alternatives rejected | Change cost now | User cost today |
| --- | --- | --- | --- | --- |
| **Report my own second `orca worktree create` in full, fix the harness, prove the fix, clean both worktrees exactly, and continue — rather than halt or quietly adopt one.** | The fault was mine: Retest 11's probe dispatches at module scope with no `__name__` guard, so importing it to inherit its hardening executed the create a second time. Hiding it would corrupt the record the run exists to produce; halting at minute two over a fixable harness bug, on a setup resource, would be a weak halt. The ground worktree is coordinator infrastructure, not one of the two certified slice lanes. | Adopt the first ground worktree and say nothing; halt the retest immediately; retry the create to read its error (the exact Retest 11 mistake). | None — both were removed at 10/10 reconstructed ownership and the baseline was re-verified before continuing. | The ground stage does not certify the one-create rule. Slices A and B do, and `create_result=1` in each create log is the check. |
| **Prove the harness fix with a fake `orca` on `PATH` before trusting it, instead of re-running the real create and looking.** | "Every counted claim carries the command that produced the number", and the claim here is *exactly one mutating call* — which is precisely the thing that must not be tested by making mutating calls. | Re-run the real create and count afterwards, which risks a third create if the fix is wrong. | None. | ~30 s and one throwaway shim. |
| **Parametrize the inherited route prover on the effort rather than rewriting it.** | The proof is the asset. Retest 10 derived the Claude matcher empirically and Retest 11 hardened it; only the effort string changed, so re-deriving would risk weakening a working proof. | Rewrite the matcher for medium; drop the effort assertion because "the banner may differ". | Two lines. | None — all six terminals matched at samples 3-4. |
| **Decline the dangerous `rm` permission dialog on Slice B's verifier rather than approve it.** | Approving a destructive `rm` on a possibly-empty variable path is not a risk QA gets to take on someone's machine, and the mutants never needed deletion. | Approve it to keep the turn alive; abandon Slice B's Technical Verification, leaving a code-changing slice unverified. | None. | Claude Code read the keystroke as an interrupt, ending that turn without a verdict — one extra follow-up packet. |
| **Complete Slice B's verification with one same-handle follow-up packet instead of a replacement agent.** | The interrupt left the lane provably clean — empty `git status`, head unchanged, no partial commit, no report — so nothing ambiguous had to be adopted, and same-handle continuation is the contract's own pattern. The follow-up repeated every original requirement and added only "do not use `rm`". | Spawn a fresh verifier terminal (a replacement actor); relax the sensor so the first partial run could count. | None. | One extra packet; nothing about the verdict or the evidence was relaxed. |
| **Satisfy `deep-review`'s `disable-model-invocation` guard with an explicit slash invocation rather than letting the agent hand-roll a review.** | The tool's own error says the workflow must not be replicated by other means; the reviewer agent was right to refuse, and the operator invoking the slash command *is* the sanctioned path. | Instruct the agent to replicate the pipeline manually — a fabricated review wearing the skill's name. | None. | One extra send; the skill then ran its real pipeline with its native fresh `deep-reviewer` subagents. |
| **Read the Deep Review verdict and every count back from the generated artifacts, not from the agent's final line.** | A reviewer reporting its own verdict is a claim standing in for a command. | Trust the `DEEP_REVIEW_DONE` marker. | None. | One extra read of `state.json`, `review-stats.json`, `findings.json`. |
| **Re-derive the newline Major's disposition from scratch instead of inheriting Retest 11's finding, and prove the test discriminates rather than merely passes.** | Retest 11 is recorded `invalid / not exercised`, and its bug record deliberately kept its contrary evidence out. A passing test is not coverage until a mutant dies against it. | Cite Retest 11 and close the bug; observe only that the suite is green. | None. | One mutation cycle on a copy proven byte-identical to the reviewed tree. |
| **Measure the host truncation myself and still leave that bug open.** | The route-around had to be distinguished from a fix. The 2012-char payload arrived complete where Retest 10's 2081-char payload lost 98.3 %, but the defect is timing-dependent, so one clean sample clears nothing. | Close the Critical because this run saw no loss; skip the measurement and inherit Retest 10's number. | None — one prompt on a worktree about to be destroyed. | One Critical stays open against the host, correctly, with the workflow-side remediation recorded as passed separately. |
| **Record `qa_status: pass` with a Critical still linked, and say plainly in the body why.** | The scenario's expected observable was met end to end with evidence at every stage. The open Critical is an upstream host defect no change in this repository can fix, and `fix_status`/`retest_status` describe the AD-018 remediation that was retested and passed. | `fail` — untrue, nothing in the journey failed. `blocked-verify` — wrong, this is not a leg only a human can walk; a session just walked it. | One frontmatter edit if the contract owner disagrees. | A reader must read one paragraph to see which bug is upstream and which was retested. |
| **Take the three Deep Review Minors as recorded, non-blocking, and unfiled.** | `REVIEW-ROUNDS.md` blocks only on `Blocker`/`Major`; all three live in a disposable fixture that was destroyed at cleanup, so a filed issue would point at nothing. | Open a round 2 for Minors; file three issues against deleted files. | None. | The Minors survive only as report prose, which is where a disposable fixture's findings belong. |
