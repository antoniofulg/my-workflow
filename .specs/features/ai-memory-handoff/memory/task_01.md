# T1 task memory

- `scripts/ai-memory.zsh` is sourceable and defines `codex` plus manual `handoff` functions.
- Tests use temporary fake `codex` and `ai-memory` executables, so no workstation configuration changes.
