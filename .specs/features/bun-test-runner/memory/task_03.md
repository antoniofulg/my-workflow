# T3 task memory

- T3 removes Vitest through npm and removes its TypeScript globals while keeping Bun native.
- npm regenerated `package-lock.json`; the package graph and dry-run package contain no Vitest.
- Changed files: `package.json`, `package-lock.json`, `tsconfig.json`, and T3 traceability state.
