# Release 0.9.1 — QA Execute

- **Date:** 2026-09-04
- **Reviewed branch / commit:** `build/release-0-9-1` at `7875bd9fd20490d660352599109add0f64094572`
- **Persona:** Repository reader and workflow adopter
- **Adapter:** Checkout-local CLI/manual through package metadata, `bun pm pack --dry-run`, `scripts/adopt.py`, `workflow_config.py --sync-agents`, and independent filesystem readback, as declared by `docs/qa/README.md`
- **Environment:** Active checkout plus session-owned disposable consumers under `docs/qa/evidence/2026-09-04-release-0-9-1/work/`
- **Raw evidence:** `docs/qa/evidence/2026-09-04-release-0-9-1/`
- **Live Orca:** not invoked
- **External skills installer:** not run

## Gate

Preflight supplied by coordinator: `bun install --frozen-lockfile` exited `0`; `bun run test:all`
exited `0` with 125/125 Bun tests plus all Python suites; `bun pm pack --dry-run
--ignore-scripts` exited `0` with 659 files and no residue.

Fresh closing `bun run test:all` exited `0`: 125/125 Bun tests across eight files and every
discovered Python suite passed, including adoption `ok (86 tests)`. No retry. Evidence:
`90-closing-gate.log`, `91-closing-gate-summary.txt`.

## Matrix

| Charter / walk | Scenarios | Verdict | Independent readback |
| --- | --- | --- | --- |
| Release 0.9.1 identity, package, 0.8.0 migration, strict sync, and cleanup | `REL-report-current-workflow-release` | pass | Clean source clone, migrated target, Git status, manifest, skill/link inventory, and independent clone readback |
| Full/layered adoption canary | `ADP-layered-workflow-adoption`; `ADP-adopt-workflow-safely` | pass | Independent cloned consumers returned clean status with the complete four-layer manifest |

## Walk results

### Release identity and package

Fresh reads agreed on `0.9.1` in `package.json`, the newest changelog heading, and
`scripts/adopt.py` `WORKFLOW_VERSION`. The manifest remains private, uses `bun@1.4.0`, has no
`package-lock.json`, and Bun's lock identifies root `my-workflow` with five dependency families.
Evidence: `10-identity-reads.txt`, `74-lock-readback.txt`.

`bun pm pack --dry-run --ignore-scripts` ran in a clean local clone of the candidate and exited
`0`: `my-workflow-0.9.1.tgz`, 659 files, 5.29 MB unpacked. It contained `scripts/adopt.py` and all
seven `w*` skill entry points; `node_modules` and `docs/qa/evidence` were absent. No archive existed
before or after. Evidence: `20-pack-dry-run.log`, `22-pack-summary.txt`.

### 0.8.0 migration and strict sync

A disposable Git consumer was adopted from local tag `v0.8.0` (`4fb602e0`) using
`apply --layers full --skip-agents`. Its clean committed baseline had a 0.8.0 four-layer manifest
and zero designer model tables.

Current strict `apply --layers full` exited `2` with
`workflow-config: models.claude.designer is required` and left Git status empty. Current
`plan --layers full --skip-agents --json` then exited `0`, reported `ready` with zero conflicts,
and left the target unchanged. Current `apply --layers full --skip-agents` exited `0`; status became
`clean`, the manifest became 0.9.1, and all seven phase skills plus their Claude links existed.
Evidence: `31-080-baseline.txt`, `33-strict-no-mutation.txt`, `34-091-plan.json`,
`35-091-apply.log`, `36-091-status.json`, `37-migration-readback.txt`.

After committing that migration checkpoint, explicit installed `workflow_config.py --sync-agents`
also exited `2` on the still-missing designer tables and left Git status empty. This confirms strict
sync still fails closed while skip-agents migration bypasses packet sync. Evidence:
`38-strict-sync.err`, `39-strict-sync-no-mutation.txt`.

### Full and layered adoption canaries

A fresh full consumer planned `ready`, applied all four layers, returned clean status, generated
all eighteen provider/role packets, and preserved consumer `package.json` and `bun.lock` hashes.
Installed Bun knowledge check exited `0`; importing the assisted probe with a call-counting fake
`orca` exited `0` with zero calls. Evidence: `41-full-plan.json`, `45-full-readback.txt`,
`62-full-runtime-canary.txt`.

A separate consumer planned and applied `core`, then `parallel`, then `quality,extras`. Core and
final statuses were clean, the final manifest contained `core,parallel,quality,extras`, and the
consumer package/lock hashes remained unchanged. Evidence: `51-layered-plan-core.json`,
`53-layered-status-core.json`, `56-layered-status-final.json`, `58-layered-readback.txt`.

Independent local clones of the full and layered targets both returned clean status with 0.9.1
four-layer manifests. The migrated legacy clone retained all seven phase skills. Evidence:
`72-independent-reload.txt`.

### 0.9.1 routing claim

Fresh source and installed bytes state the intent vocabulary and precedence: `feature` floors at
Small, `cross-feature change` floors at Medium, `direct correction` and `UI-only correction` request
the bounded fast path, and `issue` remains neutral. Qualifying UI corrections use one targeted
integration check and skip spec/tasks, Verifier, QA, deep review, repeated validation, and full E2E.
The CRM shadcn toast and TanStack/shadcn data-table examples are present. All three installed planner
templates and the installed router matched source bytes. Evidence: `73-release-claims-readback.txt`.

## Edge probes

Ten probes passed:

1. Three release identity authorities agreed on 0.9.1.
2. Bun lock root and five dependency families loaded through Bun JSONC.
3. Private dry-run pack contained 659 files and all seven phase skills.
4. Dry-run pack excluded dependency/evidence paths and left zero `.tgz` residue.
5. Strict apply on a real 0.8.0 consumer failed before writes.
6. Skip-agents plan was `ready`, conflict-free, and read-only.
7. Skip-agents apply installed seven skills and seven correct Claude links.
8. Explicit strict sync failed before writes on missing designer tables.
9. Full and incremental adoption reached clean four-layer state while preserving package bytes.
10. Independent clones, Bun knowledge, zero-effect probe import, and installed routing readback matched.

## Limitations

No browser, API, mobile, server, auth, or production runtime exists in this package. This session
did not invoke Orca, install external skills, publish, push, merge, tag, or contact GitHub/npm. Two
fixture-only command retries corrected local path/readback assumptions; neither reached or mutated a
product surface. Real Orca/Codex lifecycle and completed-pilot cleanup remain `blocked-verify`.

## Cleanup and closing gate

Session-owned disposable sources, consumers, reload clones, and fake binary under
`docs/qa/evidence/2026-09-04-release-0-9-1/work/` were removed. Clean-clone and active-checkout
`.tgz` residue was zero. Evidence: `80-active-pack-residue.txt`, `81-cleanup.txt`. The closing full
gate then exited `0` as recorded above.
