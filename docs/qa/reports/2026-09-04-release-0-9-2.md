# Release 0.9.2 — QA Execute

- **Date:** 2026-09-04
- **Reviewed branch / commit:** `fix/deep-review-defects-in-run` at `de53cb77323aa780d3fbc2d0a9b8720e3e6f7854`
- **Persona:** Repository reader
- **Adapter:** Checkout-local CLI/manual through package metadata, policy files, canonical Bun assertion, `bun pm pack --dry-run --ignore-scripts`, and independent filesystem readback, as declared by `docs/qa/README.md`
- **Environment:** Active checkout only; no consumer checkout created
- **Raw evidence:** `docs/qa/evidence/2026-09-04-release-0-9-2/`
- **Live Orca:** not invoked
- **External skills installer:** not run

## Gate

The named structural assertion passed: 1 test, 10 expectations, 0 failures. A fresh pre-close
`bun run test:all` passed on the candidate: 126/126 Bun tests across eight files and 514 Python
tests across all 21 discovered suites, with 0 failures. The first terminal run then hit one
5-second timeout in the security rollback test after 125 Bun passes. Its one permitted clean retry
passed the complete gate with the same 126 Bun and 514 Python counts. Evidence:
`20-targeted-structural-test.log`, `40-closing-full-gate.log`, `70-terminal-full-gate.log`,
`72-terminal-full-gate-retry.log`.

## Matrix

| Charter / walk | Scenarios | Verdict | Independent readback |
| --- | --- | --- | --- |
| Release 0.9.2 identity, packaged deep-review closeout contract, package residue, and unchanged adoption canary | `REL-report-current-workflow-release` | pass | Manifest, changelog, adoption version, Bun root lock entry, deep-review skill lock, release scenario/assertions, policy files, package listing, prior 0.9.1 report, and final checkout reload |

## Walk results

### Release identity

Fresh reads agreed on `0.9.2` in `package.json`, the newest changelog release heading,
`scripts/adopt.py` `WORKFLOW_VERSION`, the release scenario, and canonical version assertions.
The manifest remains private and declares `bun@1.4.0` with a Bun 1.4 engine; Bun's root workspace is
`my-workflow`, `skills-lock.json` carries the updated deep-review package hash, and
`package-lock.json` is absent. Evidence: `10-identity-readback.txt`.

### Deep-review defect closeout contract

Fresh source reads established one coherent DRC-01 through DRC-04 contract across
`docs/guidelines/REVIEW-ROUNDS.md`, `docs/workflow/reviews.md`,
`.agents/skills/autonomous/SKILL.md`, `.agents/skills/wimplement/SKILL.md`,
`.agents/skills/deep-review/references/output-contracts.md`, and `docs/workflow/pack.md`:

- Every confirmed deep-review defect closes in its originating feature run.
- Blocker and Major defects retain the next capped review round.
- Minor defects close in one current-run batch with one scoped gate and commit, with no new
  Technical Verifier, QA phase, or deep-review round.
- Cosmetics and advisories remain non-blocking follow-ups.

The single canonical structural assertion binding these installed references passed exactly as
named in the charter. Evidence: `11-contract-readback.txt`, `20-targeted-structural-test.log`.

### Package membership and residue

`bun pm pack --dry-run --ignore-scripts` exited `0` for `my-workflow-0.9.2.tgz`: 662 files,
5.32 MB unpacked. The listing contains every contract file named above plus
`tools/shared/tests/qa-skills.test.ts`. No packed `node_modules`, `docs/qa/evidence`, or archive
member appeared. Root archive snapshots were both empty, so dry-run packaging left zero `.tgz`
residue. Evidence: `30-pack-residue-before.txt`, `31-pack-dry-run.log`,
`32-pack-residue-after.txt`, `33-pack-summary.txt`.

### Unchanged adoption and migration canary

`main...HEAD` changes `scripts/adopt.py` only from workflow version 0.9.1 to 0.9.2 and changes its
canonical assertion to match; it changes no adoption or migration behavior. The immutable 0.9.1
report hash and readback retain the passing real 0.8.0 `--skip-agents` migration, 86-test adoption
suite, package membership, independent reload, and zero-residue evidence. No consumer or migration
was rerun. Evidence: `50-adoption-canary-readback.txt`; adjacent canary:
`docs/qa/reports/2026-09-04-release-0-9-1.md`.

## Edge probes

Eight probes passed:

1. All release identity authorities and canonical assertions agreed on 0.9.2.
2. The private manifest retained Bun 1.4 constraints and no npm lockfile appeared.
3. DRC-01 through DRC-04 remained coherent across all installed workflow references.
4. The named structural assertion passed with 10 contract expectations.
5. Dry-run packaging included all seven contract/test members and excluded dependency/evidence paths.
6. Before/after package archive snapshots were empty.
7. Adoption diff was version-only and the immutable 0.9.1 migration canary remained applicable.
8. Independent reload and the closing full gate matched the same candidate and durable QA state.

## Limitations

No browser, API, mobile, server, auth, or production runtime exists in this package. This session
did not invoke Orca, run adoption or migration, install external skills, publish, push, merge, tag,
or contact GitHub/npm. The real Orca/Codex lifecycle and completed-pilot cleanup remain their prior
`blocked-verify` boundaries; this release changes neither surface. The terminal gate's first Bun
attempt timed out once; the protocol's single clean retry passed, so no product defect was filed.

## Cleanup and closing gate

No session-owned consumer, clone, or package archive was created. Each passing full gate
intentionally exercised the symlink-safety sentinel and retained one exact test tree outside the
checkout; both current-run trees were moved recoverably into ignored QA-evidence quarantine. The
external pilot-directory inventory returned to its pre-terminal historical baseline. Independent
reload found zero `.tgz` files and a Git change set limited to this planned report and release
scenario closeout. The clean-retry `bun run test:all` exited `0` with the counts recorded above.
Evidence: `60-final-reload.txt`, `61-pilot-residue-baseline.txt`, `62-first-gate-cleanup.txt`,
`70-terminal-full-gate.log`, `72-terminal-full-gate-retry.log`, `73-pilot-residue-after-retry.txt`,
`74-pilot-residue-final.txt`.
