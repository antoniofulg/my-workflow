# QA report — release 0.3.6

- **Date:** 2026-08-23
- **Scope:** release 0.3.6 consistency, outcome traceability, canonical test discovery, and provenance canary
- **Adapter:** manual repository inspection plus checkout-local CLI
- **Environment:** active checkout `build/release-0.3.6` at `9000389f46e4`
- **Gate before walk:** PASS — `npm test` (8 files passed; 108 tests passed)
- **Evidence:** `docs/qa/evidence/2026-08-23-release-0-3-6/session.md`

## Matrix

| Charter | Scenario | Verdict | Independent confirmation |
| --- | --- | --- | --- |
| `CH-review-release-0-3-6-2026-08-23` | `REL-report-current-workflow-release` | pass | Reloaded `CHANGELOG.md`, both package authorities, and canonical test assertions through `git show HEAD:<path>` |
| Adjacent provenance canary | `DOC-read-explicit-workflow-provenance` | pass | Reloaded README, pack guide, both QA skill credits, and the three pinned external security entries through `git show HEAD:<path>` |

## Result

PASS. `CHANGELOG.md`, `package.json`, and both root version fields in `package-lock.json` report
`0.3.6`. The release's nine contract outcomes trace to the merged PRs or release gate commits:

1. Optional Graft/OpenDesign boundaries — PR #57.
2. Versioned feature workflow state — PR #55.
3. Trackable learnings plus immutable charters and acceptance-criterion mapping — PRs #53 and #56.
4. Explicit remote authority — PR #49.
5. Authoritative TLC `Verdict` — PR #50.
6. Effective-base and frozen-input Deep Review — PR #51.
7. Knowledge identity/date/gate separation — PR #52.
8. Idempotent walkthrough publication — PR #54.
9. Canonical Vitest discovery — release commits `7c546d4` and `c874b83`.

No product defect found. GitHub returned no open issues. The adjacent provenance canary remains
valid: bundled adaptations, upstream credits, external security boundaries, immutable pins, and
product-neutral scope remain explicit.

## Probes

- Version agreement: `CHANGELOG.md:5`, `package.json:3`, `package-lock.json:3`, and
  `package-lock.json:9` all report `0.3.6`.
- Release trace: nine outcomes map to PRs #49–#57 or the two release gate commits above.
- PR state: GitHub reports PRs #49–#57 merged with merge commits matching first-parent history.
- Canonical inventory: `git ls-files` reports exactly eight tracked Vitest files under `tools`.
- Canonical execution: `npm test` runs `vitest run --dir tools` and passes 8 files / 108 tests.
- Ignored-evidence sentinel: `git check-ignore -v` identifies `docs/qa/evidence/`; `npm test`
  remains 8/108 PASS while direct execution of the copied sentinel is 1/1 FAIL.
- Dependency drift: package and root lock dependency pins are set-equal and unchanged from `v0.3.5`;
  only release versions, test scope, and the recomputed bundled Deep Review hash changed.
- Issue state: `gh issue list --state open --limit 100 --json number,title,url` returned `[]`.
- Independent reload: committed changelog, package metadata, provenance, pack boundaries, QA credits,
  and security pins were re-read using `git show HEAD:<path>`.
- Provenance canary: README and pack stay stack/product-neutral; the three security skills remain
  separately authorized and pinned; both QA skills retain explicit local-adaptation credit.

## Final gate

PASS. Exact commands: `python3 scripts/test_adopt.py`, `python3 tools/test_ad_index.py`,
`python3 tools/test_deep_review_contract.py` (8 tests),
`python3 tools/test_deep_review_symlink_manifest.py` (5 tests),
`python3 tools/test_deep_review_token_metrics.py` (19 tests),
`python3 tools/test_workflow_config.py` (11 tests), `python3 tools/test_tlc_validators.py` (9 tests),
`npm test` (8 files / 108 tests), `npm run knowledge` (0 errors; 14 non-gating harvest warnings),
`git diff --check v0.3.5..HEAD`, and `git diff --check`.

## Limitation and residue

No browser, API, mobile, server, or live agent-execution harness exists. The declared manual/CLI
adapter can verify public repository contracts and executable gates, but cannot prove future model
compliance. No tag, release, product fix, test edit, or remote mutation is authorized in this QA
session. Raw sentinel evidence remains ignored. Durable changes are limited to this report and the
two scenario status records planned by the cycle.
