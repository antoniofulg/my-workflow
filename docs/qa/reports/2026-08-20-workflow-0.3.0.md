# QA Execute — workflow 0.3.0

- **Date:** 2026-08-20
- **Scope:** QA skills, safe adoption, provenance, artifact policy, and release metadata
- **Environment:** `feat/qa-skills` at `001ce5c`, active checkout, macOS, CLI/manual adapter
- **Adapter:** `scripts/adopt.py` against a checkout-owned disposable target; manual independent
  filesystem and repository inspection
- **Preflight gate:** `rtk env npm_config_offline=true npm test` — 55 passed, 0 failed, 0 skipped
- **Raw evidence:** `docs/qa/evidence/2026-08-20-workflow-0.3.0/session.md`
- **Limitations:** no browser, API, mobile, auth, server, or live agent-execution harness exists;
  skill behavior is verified through installed contracts and provider packets.

## Matrix

| Charter | Scenario | Verdict | Independent confirmation | Evidence |
| --- | --- | --- | --- | --- |
| `CH-adopt-qa-workflow-2026-08-20` | `ADP-adopt-workflow-safely` | pass | Re-adoption retained the profile and model-pin checksums; a separate reload showed merged consumer ignore entries. | [`session.md`](../evidence/2026-08-20-workflow-0.3.0/session.md) |
| `CH-adopt-qa-workflow-2026-08-20` | `QAS-discover-independent-qa-skills` | pass | Canonical skill files, Claude links, and all three provider Verifier packets were read after adoption. | [`session.md`](../evidence/2026-08-20-workflow-0.3.0/session.md) |
| `CH-adopt-qa-workflow-2026-08-20` | `CFG-keep-local-artifacts-out-of-git` | pass | `git check-ignore` matched generated trees while `git status` exposed learnings and durable decision files. | [`session.md`](../evidence/2026-08-20-workflow-0.3.0/session.md) |
| `CH-review-qa-release-2026-08-20` | `DOC-read-explicit-workflow-provenance` | pass | A separate repository read found all direct credits and no named consumer product or stack. | [`session.md`](../evidence/2026-08-20-workflow-0.3.0/session.md) |
| `CH-review-qa-release-2026-08-20` | `REL-report-capability-version-0-3-0` | pass | Manifest and both lockfile root declarations independently reported `0.3.0`. | [`session.md`](../evidence/2026-08-20-workflow-0.3.0/session.md) |

## Charter debriefs

### CH-adopt-qa-workflow-2026-08-20

**Verdict: pass.** Fresh adoption completed through `scripts/adopt.py`. The adopted tree exposed both
QA skills, Claude canonical links, provider Verifier packets, and a QA profile. Two passes over a
pre-populated target preserved the consumer profile and model pin byte-for-byte and produced an
idempotent merged `.gitignore`. A filled product paragraph caused the documented refusal before any
other path was copied. Independent reads after each action confirmed the observable state.

### CH-review-qa-release-2026-08-20

**Verdict: pass.** README and skill inspection distinguished Antonio Fulgêncio's adaptations from
the linked Tech Leads Club and Pedro Nauck sources. The neutral-scope scan found no prohibited
consumer product or stack names. `package.json` and the lockfile root both reported `0.3.0`.

## Edge probes and lenses

1. Fresh empty target — pass; workflow assets and default QA profile appeared.
2. Unrelated ignore entries — pass; both consumer entries survived.
3. Consumer-owned QA profile — pass; checksum unchanged across two adoptions.
4. Existing provider model pin — pass; checksum unchanged across two adoptions.
5. Filled product paragraph — pass; adoption exited `1` with the documented refusal and left the
   file intact.
6. Selective local-artifact ignores — pass; generated Deep Review and feature trees were ignored,
   while learnings and durable decisions stayed visible to Git.
7. Canonical and Claude skill discovery — pass; both skills existed and both Claude links resolved
   to `.agents/skills`.
8. Provider routing — pass; Cursor, Claude, and Codex packets dispatched separate `qa-plan` and
   `qa-execute` phases through `verifier`.
9. Provenance and neutral scope — pass; direct sources and local authorship were explicit, with no
   prohibited names found.
10. Release metadata canary — pass; all root package authorities agreed on `0.3.0`.

Lenses: comprehension passed through one documented entry point and explicit managed-path language;
recovery passed through idempotent re-adoption and safe refusal; trust passed through checksum and
provenance checks; speed passed with each adoption completing in the CLI session without a retry;
accessibility was limited to structured Markdown/CLI text because no visual surface exists; language
passed with consistent stack-neutral terminology.

## Findings

No product defects or divergences found. No bug record created.

## Final gate

`rtk env npm_config_offline=true npm test` — 5 test files passed; 55 tests passed; 0 failed;
0 skipped. A terminal-state scan found no `pending`, `untested`, or `fail` result in this report and
its five scenarios.
