# Release 0.9.0 — QA Execute

- **Date:** 2026-09-04
- **Reviewed tag / commit:** `v0.9.0` → `9e39192097c86c17d5294d147b76ddac778fef38` (annotated tag object `506d1888406f0b92579b87dfd14ae057c2dc8b8c`; equals `main` HEAD)
- **Published release:** https://github.com/antoniofulg/my-workflow/releases/tag/v0.9.0 (named by the packet; this walk did not contact GitHub)
- **Persona:** Repository reader
- **Adapter:** Checkout-local CLI/manual through package metadata, a disposable clean clone of the local repository at the tag, `scripts/adopt.py`, `workflow_config.py --sync-agents`, and independent filesystem readback, as declared by `docs/qa/README.md`
- **Environment:** QA checkout `docs/release-0.9.0-qa` at the same commit as the tag; session-owned disposable clone and consumers under `/Users/antoniofulg/Projects/.qa-release-0-9-0/` (removed at close)
- **Raw evidence:** `docs/qa/evidence/2026-09-04-release-0-9-0/`
- **Live Orca:** not invoked
- **External skills installer:** printed by adopt; not run

## Gate

Opening commands in the disposable tag clone
`/Users/antoniofulg/Projects/.qa-release-0-9-0/src` (local origin only; no remote fetch):

```text
bun install --frozen-lockfile
bun run test:all
```

`bun install --frozen-lockfile` exited `0` (Bun 1.4.0, 49 packages). Load at start
`19.99 22.17 22.74`. Evidence `20-frozen-install.log`.

`bun run test:all` exited `0`. Load at start `18.10 21.41 22.44` (1-minute load under 20).
124/124 Bun tests across 8 files; every discovered Python suite passed, including
`tools/test_parallel_resource_lock.py` 7/7. No retry. Evidence `40-test-all.log`.

## Matrix

| Charter / walk | Scenarios | Verdict | Independent readback |
| --- | --- | --- | --- |
| Release 0.9.0 identity, pack, frozen install, full gate, full-layer upgrade, and documented migration | `REL-report-current-workflow-release` | fail | Reloaded manifest, changelog heading, `WORKFLOW_VERSION`, lock identity, pack output, apply stderr, and the current scenario |

Walk result: the release identity and source-pack legs passed; documented 0.8.0→0.9.0 Migration
step 1 failed. New defect
[`BUG-20260904-adopt-apply-requires-designer-before-migration`](../bugs/BUG-20260904-adopt-apply-requires-designer-before-migration.md).
No product code was changed in this session.

## Walk results

### Release identity and private source pack

Fresh reads from the tag clone agreed on `0.9.0` in `package.json`, the newest changelog heading
`## [0.9.0] - 2026-09-04`, and `scripts/adopt.py` `WORKFLOW_VERSION`. The manifest stayed
`private: true`, `packageManager: bun@1.4.0`. Bun's lockfile identified root package
`my-workflow` and the five declared dependency families. `package-lock.json` was absent.
Canonical IT-005 expectations in `tools/shared/tests/qa-skills.test.ts` name the same version,
private flag, lock identity, and required pack members. Evidence `10-identity-reads.txt`.

An independent reload after the adoption walk repeated the same identity. Evidence
`70-independent-reload.txt`.

`bun pm pack --dry-run --ignore-scripts` exited `0`, reported `my-workflow-0.9.0.tgz`, 654 files,
and 5.24 MB unpacked. Membership included `tools/qa_parallel_pilot.py`, `tools/resource_lock.py`,
`tools/orca_assisted_probe.py`, `.agents/skills/autonomous/remediation.py`, and `scripts/adopt.py`.
`node_modules` and `docs/qa/evidence` were absent. Root `.tgz` listings on the clone and the QA
checkout were empty before and after. Evidence `30-pack-dry-run.log`.

The 0.9.0 changelog section uses heading `### Migration` (not `### Migration for 0.9.0`) and
contains two consecutive `### Changed` headings. Those are notes, not the fail.

### Documented migration (fail)

A disposable Git consumer was first adopted from a second local clone at `v0.8.0`
(`4fb602e0`) with `apply --layers full --skip-agents` (exit `0`). That write created
`.my-workflow.toml` from the 0.8.0 example (no designer tables) and 0.8.0 `templates/agents/`
(no designer files). `AGENTS.md` was seeded from `v0.8.0` so `--skip-agents` could leave it
untouched.

From the 0.9.0 clone:

| Command | Exit | Observed |
| --- | --- | --- |
| `adopt.py plan <legacy> --layers full --skip-agents --json` | `0` | `status: ready`, `conflicts: []`, 175 actions, target hash unchanged |
| `adopt.py apply <legacy> --layers full --skip-agents` | `2` | `workflow-config: models.claude.designer is required`; tree hash unchanged; no `w*` skills |
| `adopt.py status <legacy> --json` after the failed apply | `0` | `clean` |

A second 0.8.0-adopted consumer reproduced apply exit `2`. Hiding its local toml so apply used the
0.9.0 example exited `0`. Appending the three `[models.<provider>.designer]` tables from the 0.9.0
example, then applying, exited `0` with `status: clean`.

The published Migration tells operators to apply first and add designer tables in step 3. That
order does not work on a real 0.8.0 consumer. `--skip-agents` skips only `AGENTS.md` / `CLAUDE.md`
blocks; apply still runs packet sync against the consumer toml.

### Remaining migration steps (after the tables-first workaround)

These are not a pass of the documented order. They show what works once apply is unblocked.

After the workaround apply: all seven `w*` skills existed under `.agents/skills/` with
`.claude/skills/w*` links to `../../.agents/skills/<name>`. The three designer templates were
present (added as missing files; existing 0.8.0 templates were preserved). Claude `planner.md`
still lacked `skills:`. Three designer runtime packets were already present because apply had
synced after the tables were added. Adopt printed
`python3 …/scripts/install_security_skills.py <target> --yes` and this session did not run it.
Evidence `60-post-apply-inventory.txt`.

Copying `v0.9.0` `templates/agents/` over the consumer made Claude `planner.md` identical to the
source and added `skills: [workflow-spec-driven, wspecify, wtasks, ponytail]`. Changelog wording
that Claude packets also gain `disallowedTools` is only true for some roles (implementer, explorer,
deep-reviewer), not planner.

`python3 .agents/skills/workflow-config/scripts/workflow_config.py --root <legacy> --sync-agents`
exited `0`. Designer packets were unchanged and present
(`.claude/agents/designer.md`, `.codex/agents/designer.toml`, `.cursor/agents/designer.md`).
Claude planner/implementer/verifier packets gained `skills:` lines. Evidence `62-sync-agents.out`,
`63-packets-and-skills.txt`.

Hand-merging the 0.9.0 managed-block changes into the untouched 0.8.0 `AGENTS.md` succeeded: the
roles line gained `designer`, and the remote-delivery bullet gained merge-by-default plus the
up-front opt-out.

Adopted `validate_spec.py` on copies of the Large and Complex no-Impact fixtures exited `1` with
`missing required section: ## Impact`. Adding `## Impact` / `none` made both exit `0`. Evidence
`64-*.out`.

## Edge probes

10 recorded:

1. package, newest changelog heading, and `WORKFLOW_VERSION` agreed on 0.9.0 after reload — pass;
2. frozen lockfile install reported 49 packages and exit `0` — pass;
3. private dry-run pack named `my-workflow-0.9.0.tgz`, contained the five release-critical members, and left no archive residue — pass;
4. opening `bun run test:all` exited `0` without the resource-lock flake — pass;
5. 0.9.0 `plan --layers full --skip-agents` on a real 0.8.0 consumer was read-only and `ready` — pass as a preview, fail as a predictor of apply;
6. documented Migration step 1 (`apply` before designer tables) exited `2` and wrote nothing — fail;
7. apply after hiding the 0.8.0 toml, or after adding designer tables, exited `0` — workaround only;
8. seven `w*` skills and `.claude/skills/` links appeared only after the workaround apply — pass under workaround;
9. copying 0.9.0 templates then `--sync-agents` left three designer packets and `skills:` lines in `.claude/agents/` — pass;
10. Large/Complex specs without `## Impact` failed adopted `validate_spec.py`; adding the section passed — pass.

## Limitations

Real Orca/Codex lifecycle and completed-pilot cleanup remain `blocked-verify`. This session did
not invoke Orca, install external skills, publish, push, merge, retag, or contact GitHub. No
browser, API, mobile, server, auth, or production runtime exists in this package. The GitHub
Release URL was taken from the packet and not fetched.

## Cleanup and closing gate

Disposable trees `src`, `src-080`, `legacy`, and `legacy-no-toml` under
`/Users/antoniofulg/Projects/.qa-release-0-9-0/` were removed after evidence capture. No root
`.tgz` remained on the clone or the QA checkout. `scripts/install_security_skills.py` was never
executed.

Closing gate on the QA checkout after the durable report, scenario, and bug edits:

```text
bun run test:all
```

Exit `0`. Load at start `13.08 20.58 23.00`. 124/124 Bun tests across 8 files; every discovered
Python suite passed. Evidence `90-closing-gate.log`. The first spawn of this command failed with
`spawn /bin/zsh ENOENT` because the shell cwd was the already-removed clone; the recorded run
used the QA checkout as cwd.
