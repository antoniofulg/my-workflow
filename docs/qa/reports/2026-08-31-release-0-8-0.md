# Release 0.8.0 — QA Execute

- **Date:** 2026-08-31
- **Reviewed HEAD:** `0260c8cb9579998013fe429fd632ada93edff497`
- **Persona:** Repository reader
- **Adapter:** Checkout-local CLI/manual through Bun package metadata, disposable Git consumers,
  installed workflow CLIs, fake Orca, and independent filesystem/JSON readback
- **Environment:** Release-candidate checkout plus session-owned disposable Git targets
- **Raw evidence:** `docs/qa/evidence/2026-08-31-release-0-8-0/`
- **Live Orca:** not invoked

## Gate

Opening commands:

```text
npm_config_offline=true bun install --frozen-lockfile
npm_config_offline=true bun run test:all
```

Both exited `0`. Bun 1.4.0 checked 49 installs across 50 packages with no change. The mixed gate
passed 123/123 Bun tests across 8 files and all 19 discovered Python suites. Notable canonical
results included adoption 83/83, assisted probe 24/24, and resource lock 7/7.

## Matrix

| Charter | Scenarios | Verdict | Independent readback |
| --- | --- | --- | --- |
| `CH-review-release-0-8-0-2026-08-31` | `REL-report-current-workflow-release` | pass | Reloaded manifest, changelog, Bun lock, canonical assertions, pack output, and current scenario |
| Layered adoption canary | `ADP-layered-workflow-adoption` | pass | Reloaded manifest/status and compared whole-target hashes across re-adoption |
| Legacy adoption canary | `ADP-resolve-legacy-adoption-conflicts` | pass | Reloaded exact conflict set, zero-write rejection snapshot, manifest/status, and re-adoption hashes |
| First-use lock canary | `QAS-serialize-heavy-test-resources` | pass | Reloaded both child sentinels, intervals, call counts, stderr, and removed lock path |
| Offline coordination canary | `QAS-coordinate-assisted-slices-offline` | pass | Installed probe imported with fake Orca and unchanged target snapshot |
| Preserved live-host limitations | `QAS-run-resource-free-parallel-orca-slices`; `QAS-clean-owned-parallel-slice-pilot` | blocked-verify | Reloaded both status records; no live action authorized |

## Walk results

Walk result: 1/1 charter passed; 4 adjacent offline canaries remained passing and 2 live-host
scenarios remained `blocked-verify`. No product defect was found.

### Release identity and private source pack

Fresh reads agreed on `0.8.0` in `package.json`, the newest changelog heading, and canonical release
assertions. The manifest remained `private: true`. Bun's lockfile identified root package
`my-workflow` and the five declared dependency families; the frozen offline install independently
proved manifest/lock compatibility.

`bun pm pack --dry-run --ignore-scripts` exited `0`, reported `my-workflow-0.8.0.tgz`, 506 files,
and 3.98 MB unpacked. The dry-run membership included `tools/qa_parallel_pilot.py`,
`tools/resource_lock.py`, `tools/orca_assisted_probe.py`,
`.agents/skills/autonomous/remediation.py`, and `scripts/adopt.py`. `node_modules` and raw QA evidence
were absent. The root `.tgz` snapshot was empty before and after: dry-run created no archive.

Every 0.8.0 changelog claim mapped to a shipped public authority: Bun runtime (`bunfig.toml`),
modular and legacy adoption (`scripts/adopt.py`), assisted execution
(`.agents/skills/autonomous/references/parallelization.md`), machine/project locking
(`tools/resource_lock.py`), bounded remediation (`.agents/skills/autonomous/remediation.py`), and
the explicit Orca boundary (`QAS-run-resource-free-parallel-orca-slices`).

### Layered and legacy adoption

The public CLI created two independent disposable Git consumers under a session-owned temporary
root. For the layered consumer, `plan --layers core,parallel --skip-agents --json` was read-only;
`apply` resolved `core` plus `parallel`; and `status` returned `clean`. The installed pilot, lock,
probe, and remediation files existed. Consumer `AGENTS.md`, `CLAUDE.md`, `package.json`, `bun.lock`,
`.my-workflow.toml`, and QA profile survived. Whole-target hashes were identical after re-adoption.

The legacy consumer exposed exactly two sorted conflicts: `tools/orca_assisted_probe.py` and
`tools/resource_lock.py`. Resolving only one returned exit `1` and changed zero target entries.
Repeating both and only both reviewed `--replace` paths succeeded, preserved both instruction files,
reached clean managed status, and stayed byte-stable under normal re-apply.

The first harness attempt incorrectly treated source-pack-only `scripts/adopt.py` as a file installed
into a consumer. The walk stopped, removed its exact fixture, recorded the assertion, corrected only
that QA expectation, and passed on one clean retry. Package membership still independently proved
that `scripts/adopt.py` ships in the source pack.

### First-use machine lock and inert probe import

One unique machine-scoped lock was confirmed absent before use. The two installed wrappers ran from
different disposable Git repositories. Each child exited `0` and wrote exactly one call sentinel;
their measured intervals did not overlap. Combined stderr contained exactly one `wait` diagnostic.
The walk took 1.454 seconds and removed the exact lock afterward.

With a call-counting fake `orca` first on `PATH`, a fresh Python process imported the installed
`tools.orca_assisted_probe` successfully. Fake-Orca calls: 0. Whole-target mutations: 0. No live
dispatch occurred.

## Edge probes

9/9 passed:

1. package, newest changelog, scenario, and canonical assertions agreed on 0.8.0;
2. frozen offline install reported no dependency changes;
3. private dry-run pack contained all five release-critical members and no archive residue;
4. layered planning was read-only and re-adoption byte-stable;
5. consumer instructions, package metadata, local config, and QA profile survived adoption;
6. incomplete legacy resolution refused with zero writes before exact-conflict success;
7. first-use machine contention executed each child once with no interval overlap;
8. contention emitted exactly one bounded waiter diagnostic;
9. installed probe import made zero Orca calls and zero target mutations.

## Limitations

Real Orca/Codex lifecycle and completed-pilot cleanup remain external-host verification boundaries.
This session does not invoke Orca, use network access, install external skills, publish, push,
merge, tag, create a GitHub Release, or modify active CRM or Creatista checkouts.

Accordingly, `QAS-run-resource-free-parallel-orca-slices` and
`QAS-clean-owned-parallel-slice-pilot` remain `blocked-verify` with pending retests. Offline
fake-provider and import evidence do not claim a successful live lifecycle or automatic completed
pilot cleanup. No browser, API, mobile, server, auth, or production runtime exists in this package.

## Cleanup and closing gate

Both disposable consumers, fake executable, worker sentinels, and the unique lock were removed by
the walk. The opening full gate intentionally left one current-run sentinel tree at the exact path
recorded in raw evidence; inspection found only `parallel-pilot/sentinel-target/keep`, after which
that exact session-owned tree was removed. No other historical host path was touched.

Independent readback reported 22 recorded public/helper commands, all six walk categories passing,
no disposable target or lock, no release fixture process, no root tarball, and the original five-entry
Git worktree inventory.

Final command after the durable report, scenario, and spec-status edits:

```text
npm_config_offline=true bun run test:all
git diff --check
```

Exit `0`: 123/123 Bun tests across 8 files and all 19 discovered Python suites passed. Canonical
subtotals again included adoption 83/83, assisted probe 24/24, and resource lock 7/7.
`git diff --check` exited `0`.
