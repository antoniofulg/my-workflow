# QA report — release 0.6.0

- **Date:** 2026-08-25
- **Scope:** release identity, package membership, adoption, public parallel-executor claims, and
  retained Orca/Codex limitation
- **Adapter:** CLI/manual repository inspection with checkout-local disposable Git and adoption
  targets
- **Environment:** checkout
  `/Users/antoniofulg/orca/workspaces/my-workflow/feat-parallel-slice-executor`; initial HEAD
  `28798c729ac470a0427d430198e70005fc45f089`, fresh retest HEAD
  `6a9d7d4f9bbd9a277339fd829b2b012cae036489`
- **Preflight gate:** `rtk npm run test:all` — first attempt hit two independent 5-second Vitest
  timeouts with 108/110 assertions passing; the single clean retry passed 8 Vitest files / 110
  tests and every discovered Python suite.
- **Raw evidence:** `docs/qa/evidence/2026-08-25-release-0-6-0/`
- **Limitations:** no browser, API, mobile, server, runtime, port, or database surface exists in this
  repository. The real Orca/Codex two-lane lifecycle and completed-pilot cleanup remain
  `blocked-verify` and are not rerun by this release-metadata charter.

## Matrix

| Charter | Scenario | Verdict | Observable | Evidence |
| --- | --- | --- | --- | --- |
| `CH-review-release-0-6-0-2026-08-25` | `REL-report-current-workflow-release` | **pass after retest** | Release identity, package membership, adoption, resolver/fallback contracts, and bounded claims agree at `6a9d7d4` | `session.md`; fixed `BUG-20260825-adoption-omits-parallel-pilot` |
| `CH-review-release-0-6-0-2026-08-25` | `ADP-adopt-workflow-safely` | **pass after retest** | Fresh adoption installs the pilot exactly; re-adoption repairs stale managed bytes and preserves consumer config | `session.md`; fixed `BUG-20260825-adoption-omits-parallel-pilot` |

## Session

The reader entered through `CHANGELOG.md` release `0.6.0`. Independent reads agreed on version
`0.6.0` in the newest changelog heading, `package.json`, root `package-lock.json.version`, and
`package-lock.json.packages[""].version`. Canonical assertions passed in the clean full-gate retry.

`npm pack --dry-run --json` reported `my-workflow@0.6.0`, 360 entries, 706,730 packed bytes, and
2,766,664 unpacked bytes. Required executor, Orca/Git adapter, planner, resolver, convergence,
pilot, configuration, README, changelog, and adoption files were present. Local config, generated
provider runtimes, raw QA evidence, `node_modules`, and tarball residue were absent.

Fresh adoption installed the current executor, Orca/Git adapters, planner, resolver, convergence
helper, and generated provider packets. Re-adoption reported all 15 generated packets unchanged,
preserved the consumer-owned `.my-workflow.toml` SHA-256
`d8fa4216c7ee33a2f08d06f217cad14ac6b2184fdf27fdab3d3114d9c4c6bf94`, and only printed the
separately authorized external-security installer command. Independent reload then found the
release-blocking contradiction: `tools/qa_parallel_pilot.py` was absent from the adopted target
before and after re-adoption, although it is a public lifecycle entry point and is present in the
package. The session stopped before later charter probes, as required by the fix loop.

## Edge probes

1. Release identity across four authorities — pass.
2. Private package and canonical `test:all` command — pass.
3. Package required/forbidden membership — pass.
4. Fresh adoption of executor/adapters/resolver/convergence — pass.
5. Re-adoption local-config preservation and generated-packet idempotency — pass.
6. Adopted parallel-pilot entry point — **fail**; new product defect.

Remaining mode-resolution, zero-effect fallback, claim-language, authority, and final-gate probes
were not executed after the product defect. Existing real Orca/Codex and cleanup scenarios remain
`blocked-verify`; no real worker or retained fixture was touched.

## Fresh retest and resumed charter

Fresh QA resumed after independent technical PASS `6a9d7d4` for fix `816afd6`. A new disposable
target received `tools/qa_parallel_pilot.py` with exact source SHA-256
`58e8642a5cd2b01f428aceb043edaf57038828a6a7ccb2dffb0bc20ed3fc2df6`. Re-adoption repaired an
intentionally stale copy. Two re-adoptions preserved consumer-owned `.my-workflow.toml` at SHA-256
`386b848fa72bdcab4a056921d0e611b3e3fa7dceeed5061d47bc808d216c5e67` and reported all 15
generated packets unchanged.

The adjacent package canary reported private `my-workflow@0.6.0`, 361 entries, 709,758 packed bytes,
2,777,792 unpacked bytes, 14/14 selected required release files, and zero prohibited local,
generated, evidence, dependency, or tarball paths. Changelog, package, and both lockfile authorities
agreed on `0.6.0`; README and package metadata agreed on `npm run test:all`.

Three disposable resolver features froze `disabled`, `safe`, and `full` with provider `null`,
snapshot version 2, identical HEAD, deterministic grouping, and no host receipt keys. Public
effect-free executor walks then returned exact reasons `disabled-mode`, `unsupported-adapter`, and
`missing-resource-provider`, always with `actions: []` and fresh status `state: null`. The missing
provider result was identical on replay with two ready runtime-bearing lanes. Diagnostic cleanup of
each owned fixture reported zero residual paths; no completed-pilot cleanup was claimed.

The installed contracts and technical PASS support the release claims for sequential TLC task
order, provider-neutral/event-driven follow-up, checkpoint rebase and deterministic integration,
evidence invalidation, resource preflight, serial fallback, and blocker-scoped convergence. Current
durable QA still reports the real Orca/Codex lifecycle and completed-pilot cleanup as
`blocked-verify`; release prose says the same. Forty changed public/versioned prose paths since
`v0.5.0` were inspected and remained English. Autonomous authority stayed bounded to its scoped
feature-branch push, one pull request, and merge after readiness; no remote or release action ran.

## Closing gate

`rtk npm run test:all` passed on the first and only closing attempt: 8/8 Vitest files, 110/110
tests, and all 12 discovered Python suites passed. Directed suite counts included 9, 59, 45, 18,
13, 6, and 44, plus unittest groups of 10, 5, 28, and 9. `git diff --check` also passed.

## Cleanup and residue

The exact checkout-owned adoption target was removed after recording hashes and the missing path.
The fresh retest adoption target, resolver clone, and all three effect-free pilot fixtures were also
removed. The final gate's intentionally unowned sentinel fixture was removed by the QA harness after
the fail-closed assertion; all 12 gate fixture roots and derived siblings were absent afterward.
No package tarball was created. No npm publication, tag, push, pull request, merge, release,
deployment, product edit, real Orca worker action, retained-residue cleanup, external-skill install,
networked registry action, or non-disposable-target action occurred. Raw evidence remains only in
the ignored evidence directory.

## Verdict

**PASS after retest.** Fix `816afd6` closes `BUG-20260825-adoption-omits-parallel-pilot`; the
affected adoption journey and adjacent release canaries passed at `6a9d7d4`. The existing real
Orca/Codex lifecycle and completed-pilot cleanup remain `blocked-verify`, not pass or completed-pilot
evidence, and do not contradict the bounded `0.6.0` release notes.
