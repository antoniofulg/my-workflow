version = 1

[parallelization]
mode = "disabled" # disabled, safe, or full

[deep_review]
cadence = "grouped.3"

# Optional named profile. Omit this table to use one native provider for every role.
[profiles.mixed]
implementer = "claude"
verifier = "codex"
explorer = "cursor"
deep_reviewer = "codex"
