# QA report — release 0.4.0

- **Date:** 2026-08-24
- **Charter:** [`CH-review-release-0-4-0-2026-08-24`](../charters/CH-review-release-0-4-0-2026-08-24.md)
- **Persona:** Repository reader
- **Adapter:** CLI/manual repository inspection plus disposable adoption target
- **Environment:** active checkout `/Users/antoniofulg/Projects/my-workflow-ai-memory-handoff` at `dbe11cf4892649d330ebeaf008adb9b65ced7451`
- **Entry path:** `CHANGELOG.md` -> `0.4.0`; package authorities; README and `docs/workflow/ai-memory.md`; `scripts/adopt.py` against a checkout-local disposable target
- **Gate before retest:** PASS recorded by `.specs/features/release-0.4.0/validation.md`: AIM-11/AIM-12 2/2, scoped 23/23 and full 108/108 Vitest tests, all release lanes green, sensor 2/2 killed
- **Current verdict:** PASS after retest
- **Raw evidence:** `docs/qa/evidence/2026-08-24-release-0-4-0/session.md`

## Matrix

| Charter | Scenario | Verdict | Independent confirmation | Evidence |
| --- | --- | --- | --- | --- |
| `CH-review-release-0-4-0-2026-08-24` | `REL-report-current-workflow-release` | pass after retest | Working files and independent `git show HEAD:<path>` reload agree; fixed claim matches durable evidence categories | [`session.md`](../evidence/2026-08-24-release-0-4-0/session.md) |
| Adjacent handoff canary | `WFL-ai-memory-handoff` | pass preserved | Public handoff/reviewer pointers match HEAD and do not contradict the current runtime report; no lifecycle runtime walk repeated | [`2026-08-24-ai-memory-handoff.md`](2026-08-24-ai-memory-handoff.md) |
| Adjacent adoption canary | `ADP-adopt-workflow-safely` | pass | Fresh adoption/re-adoption produced the same digest, preserved consumer config, and created no ai-memory runtime artifact | [`session.md`](../evidence/2026-08-24-release-0-4-0/session.md) |

## Initial result — preserved history

Initial verdict: **FAIL**. Release identity itself passed: `CHANGELOG.md`, `package.json`, and both root
`package-lock.json` fields report `0.4.0`, and the canonical test command remains
`vitest run --dir tools`.

The mandatory lifecycle-claim probe failed. `CHANGELOG.md:20` says QA runtime walks cover the
ai-memory lifecycle-control paths. The linked durable runtime report scopes its matrix to AIM-01-AIM-08
and records no disable, re-enable, or operator purge walk. This contradicts the charter's required
support for enable/disable/re-enable/purge coverage. Filed
[`BUG-20260824-release-overstates-lifecycle-qa`](../bugs/BUG-20260824-release-overstates-lifecycle-qa.md).

The installed `ai-memory 1.31.0` help supports the documented dry-run, hook-only uninstall, and
separate destructive purge flags. The hook-only dry-run printed its planned removals and made no
requested changes. No command with `--apply`, service stop/start, shell edit, re-enable, or purge was
run.

## Fresh retest

Verdict: **PASS after fix `61f2e74` and fresh technical verification**. The old lifecycle-runtime
overclaim is absent. `CHANGELOG.md:20` limits runtime walks to handoff delivery, single-use/no
replay, the wrapper/fallback/noninteractive fix, and adoption. It separately labels lifecycle
controls documented/command-checked/dry-run only and reviewer isolation technical validation.

Release identity remained `0.4.0` in the newest changelog heading, package manifest, and both root
lockfile fields, including an independent HEAD reload. `npm_config_offline=true npm pack --dry-run
--json` reported `my-workflow@0.4.0`, 266 entries, all required release files present, and no raw
QA evidence, `node_modules`, or tarball entry.

Fresh adoption and re-adoption in a checkout-local ignored target both exited 0, produced the same
127-file SHA-256 tree digest, preserved a consumer-owned `.my-workflow.toml`, and created no
ai-memory marker or runtime artifact. The target was removed after evidence capture.

Installed `ai-memory 1.31.0` help and the public guide agree on enablement, default dry-run,
hook-only disablement, re-enablement, no repository toggle, and separately destructive purge.
The hook-only dry-run printed planned removals; documented provider hook, shell, config, and
LaunchAgent hashes/sizes were unchanged before and after. No machine lifecycle mutation ran.

The reviewer-isolation path also passed: README points internal reviewers to explicit role packets,
the ai-memory guide labels capture dropping storage/noise control only and points to
`REVIEW-ROUNDS.md`, and that authority forbids internal reviewers from consuming an Implementer
handoff while stating the top-level no-pending-Implementer condition.

## Final gate

PASS at `dbe11cf`:

- Python/AD/adoption and review/workflow/TLC lanes exited 0: 9 + 8 + 5 + 19 + 11 + 9 counted tests, with adoption and AD-index checks also green.
- `npm_config_offline=true npm ls --depth=0` found seven exact dev dependencies.
- `npm_config_offline=true npm test` passed 8 files / 108 tests / 0 skipped; `npm run knowledge` returned 0 errors / 17 known warnings.
- All four tracked spec validators and both tracked task validators returned 0 errors; three pre-existing warnings remain in `qa-skills/tasks.md`.
- Version/release assertions passed 23/23; `git diff --check v0.3.6..HEAD && git diff --check` passed.
- No dedicated scanner is declared or installed (`gitleaks` and `trufflehog` absent). A bounded high-confidence added-line scan over `v0.3.6..working-tree` found no AWS, GitHub, OpenAI, Slack, private-key, or assigned credential pattern. Limitation: heuristic scan, not an entropy/history scanner.

## Limitations and residue

No browser, API, mobile, auth, server-application, or production-health surface exists. No dedicated
secret scanner is declared or installed; the recorded scan is bounded and heuristic. Existing
`WFL` and `ADP` scenario files remain unchanged. The disposable adoption target was removed. No
product, configuration, machine lifecycle, tag, publication, commit, or remote state was changed.
