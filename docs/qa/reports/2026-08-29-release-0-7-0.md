# Release 0.7.0 — QA Execute

- **Date:** 2026-08-29
- **Reviewed HEAD:** `7ea53b5f7a5f2b6fa15d0543498b41a2066590cd`
- **Persona:** Repository reader
- **Adapter:** CLI/manual through package metadata, `npm pack --dry-run --json`, disposable
  adoption/re-adoption, installed workflow CLIs, fake providers, and independent filesystem/JSON
  readback
- **Environment:** release-candidate checkout plus checkout-local disposable Git consumer
- **Raw evidence:** `docs/qa/evidence/2026-08-29-release-0-7-0/`
- **Live Orca:** not invoked

## Gate

Pre-close command after the scenario verdict changed:

```text
npm_config_offline=true npm run test:all
```

Exit `0`: 8/8 Vitest files and 114/114 tests passed; 24 adoption checks and all 15 discovered
Python tool suites completed without failure. The exact final run after this report appears below.

## Matrix

| Charter | Scenarios | Verdict | Independent readback |
| --- | --- | --- | --- |
| `CH-review-release-0-7-0-2026-08-29` | `REL-report-current-workflow-release` | pass | Reloaded manifest, lockfile, changelog, dry-run JSON, package members, Git range, installed bytes, and scenario registry |
| Adoption canary | `ADP-adopt-workflow-safely` | pass | Reloaded 121 source-identical files; re-adoption hashes and managed probe bytes matched |
| Hybrid configuration canaries | `CFG-freeze-feature-workflow`; `CFG-plan-parallel-slice-dispatch`; `CFG-fallback-unproven-parallel-execution` | pass | Reloaded v3 snapshots, deterministic plans, fake-worker ledger, and unchanged Git worktree inventory |
| Offline coordination canary | `QAS-coordinate-assisted-slices-offline` | pass | Reloaded pointer state, transport ledger, exactly-once suite, cleanup result, and residue |
| Preserved live-host limitations | `QAS-run-resource-free-parallel-orca-slices`; `QAS-clean-owned-parallel-slice-pilot` | blocked-verify | Retained durable evidence only; no live action authorized |

## Walk results

Walk result: 1/1 charter passed; 6 adjacent offline scenarios remained passing and 2 live-host
scenarios remained `blocked-verify`. No product defect was found.

### Release identity and package

Fresh reads agreed on `0.7.0` in `package.json`, root `package-lock.json`,
`package-lock.json.packages[""]`, the newest changelog heading, canonical assertions, and dry-run
package JSON. `private: true` and `npm run test:all` remain the package authority. Local Git history
for `v0.6.0..7ea53b5` contained 67 commits and 138 changed paths, including merge commits for PRs
#70 and #73.

`npm pack --dry-run --json` exited `0` and reported 410 files, 853,788 package bytes, and 3,376,377
unpacked bytes. Twelve required members were present, including the workflow skill notice, adoption
CLI, adaptive scheduler, parallel executor, pointer probe, pilot, and all 8 Vitest plus 15 Python
tool suites used by the public gate. `node_modules`, raw QA evidence, generated provider agents,
local configuration, obsolete TLC, and tarball residue were absent. npm's normal dry-run package
membership omits the root development lockfile; its two version fields were verified directly in
the repository.

Every `0.7.0` claim mapped to shipped paths and current evidence: assisted default, serial
integration for one ready slice, isolated compatible writers, `workflow-spec-driven`, bounded
context, adaptive health, leases, pointer-only exactly-once effects, owned cleanup, and the explicit
live-Orca `blocked-verify` limitation.

### Adoption and re-adoption

The public `scripts/adopt.py` entry point installed into `/tmp/my-workflow-release-070-consumer`,
whose independent Git root was `/private/tmp/my-workflow-release-070-consumer`. Fresh readback found
121 managed files byte-identical to source. The sole transformed file was
`docs/workflow/README.md`, whose source-only `pack.md` link is intentionally removed by the adoption
contract. Seven release-critical paths were present, including `workflow-spec-driven`, scheduler,
executor, assisted probe, pilot, and CC BY notice; both obsolete TLC locations were absent.

After consumer-only sentinels changed `.my-workflow.toml` and `docs/qa/README.md`, re-adoption kept
both SHA-256 hashes exactly unchanged and restored a deliberately stale managed probe to source
bytes. Importing that installed probe in a fresh process with a call-counting fake `orca` on
`PATH` made 0 Orca calls, created no call ledger, and created no probe state.

### Hybrid and pointer canaries

Installed public resolver/planner walks produced these independently reloaded decisions:

- one ready slice: `serial-integration`, `worktree: false`;
- two compatible writers: `concurrent-writers`, 2 isolated lanes;
- overlapping `src/shared.py`: one serial lane and exact `write-conflict` blocker;
- incomplete dependency: `blocked`, 0 lanes, exact producer/in-progress reasons;
- `disabled`: one serial integration lane, no writer worktree.

The serial executor's injected offline adapter recorded exactly 1 worker mutation against
`worktree_id: integration`; worktree inventory remained one integration checkout before and after,
and the executor stopped at the expected fresh Technical Verifier boundary. An incompatible fake
Orca response failed closed as `worker-failed`, retained the correlated partial effect, and still
created no worktree.

The installed probe's public `dispatch` persisted the complete packet, emitted exactly 1 pointer
send, recorded one attempt, and put 0 occurrences of `SECRET_RELEASE_PACKET_BODY` in transport;
the body remained only in the packet file. The canonical fake-provider lifecycle exited `0` with
24/24 checks, including one physical Orca/Git/lease mutation per logical effect under transient
response, read-only reconciliation, owned cleanup, unrelated-canary preservation, and `residue: []`.

## Edge probes

10/10 passed:

1. all four release authorities reported `0.7.0`;
2. ignored runtime/evidence/dependency paths stayed out of package membership;
3. obsolete TLC paths stayed absent after adoption;
4. consumer-owned config and QA profile survived re-adoption byte-for-byte;
5. installed probe import made zero Orca calls;
6. two compatible writers planned concurrently;
7. overlapping write paths serialized with an exact reason;
8. dependency-blocked tasks produced zero lanes;
9. disabled mode and one-ready assisted mode created no writer worktree;
10. incompatible fake Orca response and transient provider responses failed closed without a
    second logical mutation.

## Limitations

Real Orca/Codex lifecycle and completed-pilot cleanup are external-host verification boundaries.
This session will not invoke Orca, publish to npm, create or push a tag, create a GitHub release,
push, open or merge a pull request, deploy, or install external skills.

No browser, API, mobile, auth, server, database, or production runtime exists. Offline providers
prove the shipped coordinator boundary but cannot convert either live-host scenario to pass.

The full gate's symlink-safety test intentionally preserved one unrelated sentinel outside its
temporary fixture. Its exact random path appeared in this run's captured output and was removed by
the QA harness after read-only ownership proof. Another 104 similarly named historical directories
outside this checkout did not match this run's output and were left untouched; they are not claimed
as release-owned residue.

### QA fixture recovery

During setup, an initial disposable directory was mistakenly created inside the candidate checkout;
Git resolved the parent repository and produced an empty commit plus one commit containing only
`.qa-release-070-consumer/**`. Read-only `git show --stat` and a path-exclusive diff proved the
scope. Recovery used `git reset --mixed 7ea53b5` (never hard), moved the exact fixture recoverably
to `/tmp`, and confirmed the source status contained only this planned untracked report. The walk
then restarted in an explicitly initialized `/tmp` Git repository. No candidate source byte, remote
ref, tag, or external system changed.

## Cleanup and closing gate

The correctly isolated consumer, focused probe fixture, and recovered misnested fixture were
removed after exact path and Git-root checks. The one current-run symlink sentinel was also removed;
the unrelated historical directories described above were not touched. Package tarball residue was
absent. Project worktree inventory was byte-identical before and after cleanup and contained exactly
2 entries: operator main and this release-candidate checkout.

Final commands and results are recorded after the last durable edit:

```text
npm_config_offline=true npm run test:all
git diff --check
git worktree list --porcelain
```

Exit `0`: 8/8 Vitest files and 114/114 tests passed; 24 adoption checks and all 15 discovered
Python tool suites completed without failure. `git diff --check` exited `0`. After exact fixture
cleanup, the project inventory still contained exactly 2 worktrees and no release tarball.
