version = 1

[deep_review]
cadence = "grouped.3"

# Optional named profile. Omit this table to use one native provider for every role.
[profiles.mixed]
implementer = "claude"
verifier = "codex"
explorer = "cursor"
deep_reviewer = "codex"
