# Bun test runner memory

- Bun 1.4.0 is available locally; npm remains the package, lockfile, and publishing owner.
- The structural test boundary is the repository-local `bunfig.toml` root at `./tools`.
